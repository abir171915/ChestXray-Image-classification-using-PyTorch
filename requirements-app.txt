# Runtime dependencies for the deployed API only — deliberately smaller than
# requirements.txt (which also covers notebook work: matplotlib, torchinfo,
# scikit-learn, pytest). Keeping these separate keeps the Docker image lean.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.9.1+cpu
torchvision==0.24.1+cpu
fastapi==0.135.1
uvicorn==0.41.0
python-multipart
pydantic
Pillow==10.1.0
